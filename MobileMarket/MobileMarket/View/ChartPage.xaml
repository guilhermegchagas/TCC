<?xml version="1.0" encoding="utf-8" ?>
<TabbedPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:chart="clr-namespace:Syncfusion.SfChart.XForms;assembly=Syncfusion.SfChart.XForms"
             xmlns:viewModel="clr-namespace:MobileMarket.ViewModel"
             xmlns:local="clr-namespace:MobileMarket.View"
             mc:Ignorable="d"
             x:Class="MobileMarket.View.ChartPage"
             Title="{Binding ponto.Nome}">
    <TabbedPage.BindingContext>
        <viewModel:ChartPageViewModel/>
    </TabbedPage.BindingContext>
    <ContentPage Title="Gráfico" IconImageSource="chart_icon.png">
        <ContentPage.Content>
            <StackLayout Orientation="Vertical">
                <StackLayout Orientation="Horizontal">
                    <Label HeightRequest="30" Text="{Binding DataInicio.Date, StringFormat='{0:MMMM dd, yyyy}'}"></Label>
                    <Label HeightRequest="30" Text="{Binding DataInicio.TimeOfDay, StringFormat='{}{0:hh\\:mm}'}"></Label>
                    <Button ImageSource="table_icon.png" WidthRequest="30" HeightRequest="30" Clicked="DataInicio_Clicked"/>
                    <local:DateTimePicker
                        x:Name="dataInicioControl"
                        ColumnHeaderHeight="40"
                        HorizontalOptions="Start"
                        PickerHeight="400"
                        PickerMode="Dialog"
                        PickerWidth="300"
                        SelectedItem="{Binding DataInicioCollection, Mode=TwoWay}"
                        VerticalOptions="Center" />
                    <Label HeightRequest="30" Text="{Binding DataFim.Date, StringFormat='{0:MMMM dd, yyyy}'}"></Label>
                    <Label HeightRequest="30" Text="{Binding DataFim.TimeOfDay, StringFormat='{}{0:hh\\:mm}'}"></Label>
                    <Button ImageSource="table_icon.png" WidthRequest="30" HeightRequest="30" Clicked="DataFim_Clicked"/>
                    <local:DateTimePicker
                        x:Name="dataFimControl"
                        ColumnHeaderHeight="40"
                        HorizontalOptions="Start"
                        PickerHeight="400"
                        PickerMode="Dialog"
                        PickerWidth="300"
                        SelectedItem="{Binding DataFimCollection, Mode=TwoWay}"
                        VerticalOptions="Center" />
                </StackLayout>
                <chart:SfChart 
                        Margin="5"
                        HorizontalOptions="FillAndExpand" 
                        VerticalOptions="FillAndExpand">
                    <chart:SfChart.PrimaryAxis>
                        <chart:DateTimeAxis/>
                    </chart:SfChart.PrimaryAxis>
                    <chart:SfChart.SecondaryAxis>
                        <chart:NumericalAxis/>
                    </chart:SfChart.SecondaryAxis>
                    <chart:SfChart.Series>
                        <chart:FastLineSeries 
                                ItemsSource="{Binding Medicoes}" 
                                XBindingPath="Horario" 
	 					        YBindingPath="Potencia"/>
                    </chart:SfChart.Series>
                </chart:SfChart>
            </StackLayout>
        </ContentPage.Content>
    </ContentPage>
    <ContentPage Title="Dados" IconImageSource="table_icon.png">
    </ContentPage>
</TabbedPage>